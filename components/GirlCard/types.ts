export type girlCardType = {
    image?: any,
    verified?: boolean,
    online?: boolean,
    name?: string,
    location?: string,
    photoCount?: number,
    age?: string | number,
    link?: URL
}